<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bilgerat.wizzy.avast" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activities.StartupActivity"
            android:label="@string/title_activity_startup" >
        </activity>
        <activity
            android:name=".Activities.DesignAppearanceActivity"
            android:label="@string/title_activity_design_appearance" >
        </activity>
        <activity
            android:name=".Activities.DesignParamsActivity"
            android:label="@string/title_activity_design_params" >
        </activity>
        <activity
            android:name=".Activities.MapActivity"
            android:label="@string/title_activity_map" >
        </activity>
        <activity
            android:name=".Activities.SelfViewActivity"
            android:label="@string/title_activity_self_view" >
        </activity>

        <service
            android:name=".Activities.BackgroundService"
            android:enabled="true"
            android:exported="true" >
        </service>
    </application>

</manifest>
